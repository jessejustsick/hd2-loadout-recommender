# Supabase project credentials (client-safe; the anon key is protected by Row Level Security).
# Copy to .env and fill in from Supabase → Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
